// GoHaSeMo Core SDK — Apache-2.0.
//
// Pure-Go, stdlib-only: there is intentionally NO require block (no
// third-party dependencies). Keep it that way (ADR-008 import isolation):
// the SDK depends only on the Go standard library.

module gohasemo.com/core

go 1.25.0
